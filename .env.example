# ── Token Storage ──────────────────────────────────────────────────────────────
# Persist design tokens to a dedicated GitHub repo so developers don't need to
# manually upload token files. Both variables are required for storage to work.
# If omitted, the app works in manual-upload mode.

# GitHub Personal Access Token with `repo` scope (server-side only — NOT the
# client-side PAT used for PR creation)
TOKENS_GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Owner/repo for the dedicated token storage repository
TOKENS_GITHUB_REPO=ishanjalan/design-tokens

# ── Google Chat Notification ───────────────────────────────────────────────────
# Webhook URL for posting design token update summaries to Google Chat.
# Only fires when auto-sync detects changes (not on manual generation).
# If omitted, notifications are disabled unless overridden in Settings.
GOOGLE_CHAT_WEBHOOK_URL=https://chat.googleapis.com/v1/spaces/…/messages?key=…&token=…
